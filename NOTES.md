# attach to a running pod without shell 
> kubectl alpha debug -it ephemeral-demo --image=busybox --target=ephemeral-demo
> https://kubernetes.io/docs/tasks/debug-application-cluster/debug-running-pod/
